[project]
name = "pathfinder-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyjwt>=2.8,<3",
    "python-docx>=1.2,<2",
]

[tool.pytest.ini_options]
pythonpath = ["backend"]
testpaths = ["backend/tests"]
